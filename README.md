# salah-sync

Automatically adds Islamic prayer times to your Google Calendar based on your location.

## Running locally

Google sign-in should be tested from an authorized origin, not by opening the
HTML file directly in the browser.

One simple option is:

```bash
python3 -m http.server 3000
```

Then open [http://localhost:3000](http://localhost:3000).
